### PURPOSE FOR EACH FILE
- `Factorial.cpp`

To find whether a non-negative integer is a factorial number or not a factorial number.

- `Prime Number.cpp`

To determine whether a non-negative integer is a prime number or not a prime number.
